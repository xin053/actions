name: test
on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: test1
        run: echo "1" > $GITHUB_WORKSPACE/rocky9.tar.gz
      - name: Upload to release file
        uses: softprops/action-gh-release@v2
        with:
          name: UserBuild_2024.04.26_11
          tag_name: UserBuild_2024.04.26_11
            ./*.gz
